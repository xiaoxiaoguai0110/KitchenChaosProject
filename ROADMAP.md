# KitchenChaos 打磨路线图

本文件记录项目从“玩法完整”走向“可稳定发布”的改进计划。后续实现任务时，请更新复选框，并在条目下补充对应 PR、测试结果或设计决定。

## 状态说明

- `[ ]` 尚未开始
- `[~]` 正在进行
- `[x]` 已完成并验证
- 优先级：P0 阻塞体验或稳定性，P1 提升手感，P2 增加内容，P3 工程保障

## 当前基线

- Unity：`2022.3.62f1c1`
- 核心循环：拿取食材 → 加工 → 装盘 → 交付
- 模式现状：主菜单已支持独自挑战、AI 协作和本地双人，GameManager 会按模式分配控制权
- AI：已拆分目标选择、订单规划、动作和移动模块；移动模块已接入 NavMesh，并具备卡住重规划、目标超时和玩家避让
- 移动：真人和 AI 已迁移到 `CharacterController.Move()`，仍需完成完整实机回归
- 自动化：具备 Windows 构建工作流；测试覆盖仍很少

---

## P0：玩法正确性与稳定性

### 1. 明确单人、AI 协作和双人模式

- [x] 主菜单增加“单人模式”和“本地双人”入口
- [x] 单人模式可选择独自挑战或启用 AI 厨师
- [x] 独自挑战禁用 Player 2 游戏对象
- [x] AI 协作模式禁用 Player 2 真人输入，只启用 AI
- [x] 双人模式禁用 AI，只启用 Player 2 真人输入
- [x] GameManager 不再无条件启用 Player 2 控制脚本

实现记录（2026-08-17）：菜单入口与控制权切换已完成，ICE 菜单字体已改为动态多图集；仍需分别游玩独自挑战、AI 协作和本地双人模式完成最终验收。

验收：两种模式互不抢占控制权，切换场景和重新开始后状态仍正确。

### 2. AI 寻路与脱困

- [x] 选择路径点网络、网格寻路或 NavMesh 方案
- [x] AI 不再只沿直线穿越厨房布局
- [~] 检测连续数秒没有产生有效位移
- [~] 卡住时重新规划路径或更换目标
- [~] 目标柜台增加到达超时
- [~] 动态避让玩家和繁忙通道

实现记录（2026-08-19）：NavMesh 只负责规划和局部避障，角色仍由 `CharacterController.Move()` 执行真实位移。AI 每 `0.08m` 记录一次有效进展；连续 `2s` 无进展时重新采样柜台站位，最多重试 `2` 次；追逐同一目标超过 `12s` 会放弃。真人停下 `0.5s` 后以 Carving 障碍参与重规划，真人占用柜台时 AI 会短暂排除该柜台。Unity 冒烟测试通过：20 秒内移动 `54.25m`，完整路径、路径拐点、NavMesh 贴合及真人动态障碍均正常。带 `[~]` 的条目已完成代码实现，仍需按下方 10 局场景清单完成最终实机验收。

验收：让 AI 连续运行至少 10 局，不出现永久卡墙或无限等待。

### 3. 修复事件生命周期

- [x] 移除 `ClearStaticData.Start()` 的不确定清理时序
- [x] 静态事件改用 `RuntimeInitializeOnLoadMethod(SubsystemRegistration)` 重置
- [x] 普通事件统一在 `OnEnable` 订阅、`OnDisable` 解绑
- [x] 检查 UI、OrderManager、SoundManager、GameManager 的所有订阅
- [~] 防止重进场景后重复响应事件

实现记录（2026-08-19）：`KitchenObjectHolder`、`CuttingCounter` 和 `TrashCounter` 的静态事件已由各自拥有者在 `SubsystemRegistration` 阶段自动重置；`ClearStaticData` 不再使用 `Start()` 清理。GameInput、Player、AI、Manager、游戏 UI 与按钮监听已改为 `OnEnable`/`OnDisable` 对称管理，并保存实际订阅源，避免场景切换时向错误的单例解绑。新增 3 项 EditMode 生命周期测试，结果 `3 passed / 0 failed`。仍需完成反复进入游戏、返回菜单和重新开始至少 20 次的人工验收，完成后将最后一项改为 `[x]`。

验收：反复开始、退出、重新开始至少 20 次，没有重复音效、重复 UI 或残留回调。

### 4. 游戏状态统一控制模拟

- [ ] GameOver 后停止生成订单
- [ ] GameOver 后停止炉灶计时和盘子生成
- [ ] 非 GamePlaying 状态禁止柜台继续推进加工
- [ ] 限制暂停仅在允许的状态触发
- [ ] 切换场景前确保 `Time.timeScale = 1`

验收：倒计时、暂停和结算期间没有隐藏的订单或加工状态变化。

### 5. 改进交互检测

- [ ] Raycast 改为 SphereCast、CapsuleCast 或候选柜台评分
- [ ] 综合距离和朝向选择最佳柜台
- [ ] 显示当前可交互目标和按键提示
- [ ] 非法交互提供视觉或声音反馈
- [ ] 检查两个玩家同时选择同一柜台时的显示状态

验收：贴近柜台、斜向面对柜台和快速转身时都能稳定选中预期目标。

### 6. CharacterController 实机回归

- [x] 真人和 AI 统一通过 `CharacterController.Move()` 移动
- [x] 场景角色移除 Rigidbody 和独立 CapsuleCollider
- [x] 增加场景组件 EditMode 测试
- [ ] 测试贴墙斜向移动和碰撞滑动
- [ ] 测试两名玩家相互穿越/阻挡规则
- [ ] 测试 30、60、120 FPS 下的移动一致性
- [ ] 测试角色是否意外爬上低矮碰撞体或悬空

验收：无抖动、穿透、卡边或明显帧率相关移动差异。

---

## P1：操作手感与反馈

### 7. 移动手感

- [ ] 增加短暂加速与减速
- [ ] 调整转向速度和移动动画混合
- [ ] 根据地面材质或速度改变脚步反馈

### 8. 交互反馈

- [ ] 拾取、放下物品增加缩放或弹跳动画
- [ ] 切菜增加粒子、刀具反馈和轻微镜头震动
- [ ] 炉灶完成与即将烧焦使用不同提示
- [ ] 送餐成功增加分数跳字和庆祝效果
- [ ] 错误操作增加清晰但不打扰的反馈

### 9. 炉灶警告

- [ ] 警告频率随烧焦进度逐渐加快
- [ ] 避免固定每 0.2 秒播放警告音
- [ ] 完成、警告、烧焦使用不同视觉与声音语言

### 10. 音效系统

- [ ] 倒计时使用独立音效，不复用炉灶警告音
- [ ] 脚步、切菜和放置音效增加多个变体
- [ ] 添加轻微随机音高
- [ ] 限制相同音效同时播放数量
- [ ] 评估 AudioSource 池，减少 `PlayClipAtPoint` 分配

### 11. UI 分辨率适配

- [ ] Canvas 改为 `Scale With Screen Size`
- [ ] 以 1920×1080 建立参考分辨率
- [ ] 测试 16:9、16:10、超宽屏和窗口模式
- [ ] 检查安全区、文字截断和订单列表溢出

### 12. 暂停与结算界面

- [ ] 增加重新开始和返回主菜单
- [ ] 显示成功订单、失败订单和最终分数
- [ ] 增加最佳成绩与星级评价
- [ ] 结算界面支持键盘与手柄导航

### 13. 输入系统

- [ ] 两名玩家都迁移到 Input System Action Map
- [ ] 支持键盘双人和手柄
- [ ] Player 2 支持改键
- [ ] 根据输入设备显示对应按键图标
- [ ] 解决没有数字小键盘时 Player 2 无法操作的问题

### 14. 可访问性

- [ ] 音乐和音效改为滑块
- [ ] 增加屏幕震动开关
- [ ] 增加文字大小设置
- [ ] 使用颜色和图标共同表达订单状态
- [ ] 评估长按/连按操作替代方案

---

## P2：难度、内容与重复游玩

### 15. 难度曲线

- [ ] 随时间调整订单生成间隔
- [ ] 逐步增加复杂菜谱概率
- [ ] 控制同时存在的订单数量
- [ ] 提供休闲、标准和挑战难度
- [ ] 将局时、订单速度和烧焦时间数据化

### 16. 计分系统

- [ ] 基础订单分
- [ ] 剩余时间奖励
- [ ] 连续成功 Combo
- [ ] 错单扣分和浪费惩罚
- [ ] 一至三星评级
- [ ] 本地最佳成绩保存

### 17. 更多关卡布局

- [ ] 狭窄通道厨房
- [ ] 分区协作厨房
- [ ] 移动柜台或传送带
- [ ] 需要相互传递食材的布局
- [ ] 为每个布局设计独立难度和教学节奏

### 18. 扩充菜谱与加工

- [ ] 多阶段烹饪
- [ ] 洗盘子
- [ ] 搅拌、烘焙或煮汤
- [ ] 特殊限时订单
- [ ] 评估可替代食材和菜谱变体

### 19. AI 协作指令

- [ ] 跟随玩家
- [ ] 优先切菜、烹饪或装盘
- [ ] 禁止处理指定食材
- [ ] 在柜台上放置任务标记
- [ ] 提供休闲、标准、高效 AI 难度

---

## P3：工程质量与发布保障

### 20. 编码与日志清理

- [ ] 所有脚本统一为 UTF-8
- [ ] 修复乱码注释、错误和日志
- [ ] 删除 `print("11")` 等临时调试输出
- [ ] 建立开发日志等级或条件编译策略

### 21. 命名与代码规范

- [ ] `IsHaveKitchenObject` 改为 `HasKitchenObject`
- [ ] `Successed` 改为 `Succeeded`
- [ ] `spawRate` 改为 `spawnRate`
- [ ] 删除无用 using、空 Update 和空 else
- [ ] 统一字段、空格、大括号和访问修饰符风格

### 22. 依赖与全局状态

- [ ] Singleton 检测重复实例并明确生命周期
- [ ] `GetOrderList()` 返回只读集合
- [ ] 减少 UI 和业务逻辑对全局 Instance 的直接依赖
- [ ] 为运行时代码增加 asmdef 和命名空间

### 23. 参数数据化

- [ ] 将游戏时长和倒计时移入配置
- [ ] 将订单速度、最大订单数移入配置
- [ ] 将移动速度、加工时间和警告阈值移入配置
- [ ] 为不同难度建立 ScriptableObject 配置

### 24. 自动化测试

- [ ] KitchenObject 转移与销毁测试
- [ ] 盘子食材添加规则测试
- [ ] 订单匹配测试
- [ ] 炉灶状态转换测试
- [ ] AI 原料追溯测试
- [ ] AI 目标选择与脱困测试
- [ ] 游戏倒计时、暂停和结束测试
- [ ] 主菜单到游戏再到结算的 PlayMode 冒烟测试

### 25. CI/CD

- [ ] GitHub Actions 在构建前运行 EditMode 测试
- [ ] GitHub Actions 运行关键 PlayMode 测试
- [ ] 测试失败时禁止生成发布包
- [ ] 为发布版本生成带版本号的构建产物

### 26. 性能基线

- [ ] 记录目标硬件和目标帧率
- [ ] 使用 Profiler 检查 GC Alloc
- [ ] 检查 UI Rebuild、音效实例和 AI 临时集合
- [ ] 检查 Draw Calls、阴影和粒子开销
- [ ] 保存优化前后的基准数据

---

## 推荐实施顺序

1. 单人/AI 协作/双人模式与控制权（实现完成，待最终游玩验收）
2. AI 寻路、卡住检测和目标超时
3. 静态事件及生命周期
4. GameOver/暂停时停止模拟
5. 交互检测与 CharacterController 实机回归
6. 操作反馈、音效和 UI 适配
7. 输入设备与可访问性
8. 难度、计分、关卡和菜谱内容
9. 测试、CI 与性能基线

## 每项任务的完成标准

任务只有同时满足以下条件才标记为 `[x]`：

- 功能在 Unity Editor 中完成实际游玩验证
- 没有新增编译错误或 Console 异常
- 相关 EditMode/PlayMode 测试通过，或记录无法自动化的原因
- README、ROADMAP 或 Inspector 配置说明已同步
- 变更通过独立 commit 或 PR 留下可追溯记录
